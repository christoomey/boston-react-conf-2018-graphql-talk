import styled from 'styled-components';
import {
  RepoForkedIcon,
  StarIcon as Star,
} from 'react-octicons';

export const StarIcon = styled(Star)`
  margin-right: 0.25em;
`;

export const ForkedIcon = styled(RepoForkedIcon)`
  margin-right: 0.25em;
`;
